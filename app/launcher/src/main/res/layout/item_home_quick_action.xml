<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:minHeight="120dp"
    app:cardBackgroundColor="@color/ezivia_primary"
    app:cardCornerRadius="28dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:id="@+id/quickActionIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:contentDescription="@null"
            android:tint="@android:color/white"
            tools:src="@drawable/ic_action_call" />

        <TextView
            android:id="@+id/quickActionTitle"
            style="@style/TextAppearance.Ezivia.Display"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="@android:color/white"
            tools:text="Llamadas" />

        <TextView
            android:id="@+id/quickActionDescription"
            style="@style/TextAppearance.Ezivia.BodyLarge"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@android:color/white"
            tools:text="Contacta en dos toques" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
