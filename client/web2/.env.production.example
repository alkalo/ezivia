# Variables de entorno utilizadas durante el build de la web secundaria de TapTop.
# Copia este archivo a `.env.production` antes de ejecutar `npm run build` si necesitas
# ajustar valores en el VPS.

# URL base para APIs externas consumidas por la web. Debe incluir protocolo.
VITE_API_BASE_URL=https://api.taptop.es

# Indica si se deben habilitar integraciones de analítica.
VITE_ENABLE_ANALYTICS=false
