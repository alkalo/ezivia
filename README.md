# ezivia
Conversor de moviles para mayores


# Debug (ya compila, por si quieres rehacerla limpia)
.\gradlew.bat --no-daemon :launcher:assembleDebug

# Release (normalmente sin firmar)
.\gradlew.bat --no-daemon :launcher:assembleRelease
